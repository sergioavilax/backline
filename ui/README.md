# Backline UI

Next.js 15 + TypeScript + Tailwind (pnpm). Phase 0 placeholder — the four surfaces
(Chat, Trace Inspector, Review Queue, Eval Dashboard) ship in Phase 6 per
[BUILD_PLAN.md](../BUILD_PLAN.md) §6 and `docs/UI_DIRECTION.md`.

```bash
pnpm install
pnpm dev    # http://localhost:3000
pnpm lint
pnpm build
```
