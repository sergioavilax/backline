[project]
name = "backline"
version = "0.1.0"
description = "Agent platform for music label operations — contracts, catalog, and royalty statements."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "asyncpg>=0.30",
    "pydantic>=2.7",
    "pydantic-settings>=2.4",
    "numpy>=2.1",
    "pyyaml>=6.0",
    "reportlab>=4.2",
    "tiktoken>=0.8",
    "anthropic>=0.120",
    "httpx>=0.27",
    "sqlglot>=27.29",
]

# The real embedding/rerank models (bge-small-en-v1.5 + ms-marco cross-encoder) run
# CPU-side via sentence-transformers — a heavy dependency (torch). It is optional so
# keyless CI and unit tests stay light; the Docker image and dev machines that build
# real embeddings install `--extra embed`. Offline environments use EMBED_MODEL=hash.
# torch is declared here (not just transitively via sentence-transformers) so the
# pytorch-cpu index pin below binds it: PyPI's linux torch wheels are CUDA builds,
# while this stack is CPU-only by design (D-011).
[project.optional-dependencies]
embed = ["sentence-transformers>=3.2", "torch>=2.2"]

# CPU-only torch (the D-011 re-lock): resolve torch from the PyTorch CPU wheel index
# instead of PyPI, so `--extra embed` installs ~200 MB of CPU wheels instead of ~5 GB
# of CUDA builds and the Docker image can ship real embedding models. The block is
# staged here but commented out because build sandboxes cannot reach
# download.pytorch.org (403 host_not_allowed — verified 2026-08-06, see PHASE_LOG
# Phase 4 pre-tasks) and an index pin the environment can't fetch would break every
# implicit `uv lock`. On a network that reaches it: uncomment, run `uv lock`, and add
# `--extra embed` to both `uv sync` lines in docker/api.Dockerfile.
#
[tool.uv.sources]
torch = [{ index = "pytorch-cpu" }]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "ruff>=0.6",
    "mypy>=1.11",
    "hypothesis>=6.100",
    "pytest-cov>=5.0",
    "types-pyyaml>=6.0",
    "types-reportlab>=4.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backline", "datagen"]

[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = ["ui", "*.md"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # bugbear
    "SIM", # simplify
    "RUF", # ruff-specific
]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["backline", "datagen", "scripts", "tests", "evals"]

[[tool.mypy.overrides]]
module = "asyncpg.*"
ignore_missing_imports = true

# Optional heavy dependency (see [project.optional-dependencies].embed): imported
# lazily inside backline/rag; not installed in keyless CI.
[[tool.mypy.overrides]]
module = "sentence_transformers.*"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
# `live` tests hit the real Anthropic API and cost money; they are excluded by default
# and run manually via `uv run pytest -m live` (BUILD_PLAN Phase 2 DoD). A later -m on
# the command line overrides this default.
addopts = '-m "not live"'
markers = [
    "live: hits the real Anthropic API (needs ANTHROPIC_API_KEY; run manually with -m live)",
]
